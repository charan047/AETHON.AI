VITE_API_URL=http://localhost:8000
# Override WebSocket URL for deployments where API is on a different domain
# Leave empty for auto-detect (uses same host as the frontend)
VITE_WS_URL=
# Example production value: wss://api.yourdomain.com
